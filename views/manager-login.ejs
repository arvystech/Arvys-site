<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Manager Login | ARVYS</title>
    <link href="/assets/vendors/bootstrap/bootstrap.min.css" rel="stylesheet" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <link href="/assets/css/style.css" rel="stylesheet" />
    <style>
      body {
        background: linear-gradient(135deg, #0016c2 0%, #4444b5 100%);
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0;
        font-family: "Inter", sans-serif;
      }
      .login-card {
        background: rgba(255, 255, 255, 0.95);
        backdrop-filter: blur(10px);
        padding: 3rem;
        border-radius: 24px;
        box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 400px;
        text-align: center;
      }
      .logo-img {
        max-width: 120px;
        margin-bottom: 2rem;
      }
      .form-control {
        border-radius: 12px;
        padding: 0.8rem 1.2rem;
        border: 2px solid #eee;
        transition: all 0.3s ease;
      }
      .form-control:focus {
        border-color: #0016c2;
        box-shadow: 0 0 0 4px rgba(0, 22, 194, 0.1);
      }
      .btn-login {
        background: #0016c2;
        color: white;
        border: none;
        border-radius: 12px;
        padding: 0.8rem;
        font-weight: 600;
        width: 100%;
        margin-top: 1.5rem;
        transition: all 0.3s ease;
      }
      .btn-login:hover {
        background: #0012a3;
        transform: translateY(-2px);
        box-shadow: 0 10px 20px rgba(0, 22, 194, 0.2);
        color: white;
      }
      .error-msg {
        color: #dc3545;
        font-size: 0.9rem;
        margin-top: 1rem;
        background: rgba(220, 53, 69, 0.1);
        padding: 0.5rem;
        border-radius: 8px;
      }
    </style>
  </head>
  <body>
    <div class="login-card">
      <img
        src="/assets/public/IMAGES/logos/logo.svg"
        alt="ARVYS"
        class="logo-img"
      />
      <h4 class="fw-bold mb-4">Manager Access</h4>
      <form action="/manager/login" method="POST">
        <div class="mb-3">
          <input
            type="password"
            name="passphrase"
            class="form-control"
            placeholder="Enter Passphrase"
            required
            autofocus
          />
        </div>
        <button type="submit" class="btn btn-login">UNLOCK DASHBOARD</button>
        <div
          class="error-msg"
          style="display: <%= typeof error !== 'undefined' ? 'block' : 'none' %>;"
        >
          <%= typeof error !== 'undefined' ? error : '' %>
        </div>
      </form>
    </div>
  </body>
</html>
